// nomaflow feature area — the custom job-authoring + control UI (NOMAFLOW-UI.md).
// Routed at /nomaflow (Jobs list) and /nomaflow/jobs/:id (Job editor); see App.tsx.
// The default export is the Jobs list — the feature-area home.
export { default } from './JobsList'
